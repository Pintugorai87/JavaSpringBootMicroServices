server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pintugorai87/spring-app-config.git
          cloneOnStart: true
